{
  "name": "hostelero",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build:consola": "npm run build --workspace apps/consola",
    "dev:consola": "npm run dev --workspace apps/consola"
  }
}
